import React from "react";
import MDSpinner from "react-md-spinner";

const Spinner = () => {
  return (
    <div>
      <MDSpinner size={70} singleColor="#7b61ff" />
    </div>
  );
};

export default Spinner;
